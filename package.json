{
  "name": "telegram_video_to_animated_sticker",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram Video to Animated Sticker / Emoji Converter Web Studio",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
